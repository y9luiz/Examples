# Como compilar?

    * Instale o cmake
        `sudo apt-get install cmake`
    * Instale a asio
        `sudo apt-get install libasio-dev`
    * Crie uma pasta build
        mkdir build
    * Agora digite
        cmake ..
        make
        ./server
        ./client